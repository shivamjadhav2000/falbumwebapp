<div className='w-5/6 h-5/6  bg-slate-600 absolute shadow-md top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 '>
       { currentAlbum && <div>
                            <div className='flex flex-row-reverse p-2'>
                                <button onClick={()=>{setmediaView(false)}}>close</button>
                            </div>
                            <div className='absolute w-full h-full flex justify-center items-center'>
                            <ChevronLeftIcon sx={{ fontSize: 60 }} onClick={()=>{handleimgRotation('-')}} />
                            <img
                                src={`http://localhost:3000/uploads/${currentAlbum.images[currentMedia]}`}
                                alt={currentAlbum.images[currentMedia]}
                                className='cursor-pointer hover:shadow-2xl' 
                                style={{
                                    width: "80%",
                                    height: "80%",
                                    objectFit: "cover",
                                }}                   
                            />
                            <ChevronRightIcon sx={{ fontSize: 60 }} onClick={()=>{handleimgRotation('+')}}/>
                            </div>
                            </div>
        }
    </div>